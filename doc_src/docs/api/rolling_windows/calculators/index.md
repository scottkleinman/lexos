# Rolling Windows Calculators

The `rolling_windows` module has three built-in calculator classes, [counts](counts.md), [averages](averages.md), and [ratios](ratios.md). Custom calculators should inherit from [base_plotter](base_calculator.md).
